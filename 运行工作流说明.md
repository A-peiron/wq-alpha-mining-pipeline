# 用户阶段运行工作流说明（不使用 `submit_alpha.py`）

这份说明适用于你当前这版代码：
- 已接入三槽调度（探索/开发/验证或利用）
- `check.py` 已按 `SELF_CORR_THRESHOLD` 做本地预筛
- 你决定不用 `submit_alpha.py`，只做“挖掘 + 检查 + 人工提交”

---

## 1. 先改好这几个配置

### 1.1 step1 配置
文件：`digging_1step.py`

重点参数：
- `dataset_id`（比如 `fundamental6`）
- `step1_tag`
- `EARLY_SCORE_MODE`

建议：
- 冲分期：`EARLY_SCORE_MODE = True`
- 提质期：`EARLY_SCORE_MODE = False`

### 1.2 step2 配置
文件：`digging_2step.py`

重点参数：
- `step1_tag`（必须和 step1 一致）
- `step2_tag`
- `EARLY_SCORE_MODE`（建议与 step1 同步）

### 1.3 check 配置
文件：`config.py`

重点参数：
- `SELF_CORR_THRESHOLD = 0.75`

说明：这是本地预筛阈值，不是平台最终判定；用于提高筛选效率。

---

## 2. 日常执行顺序（不用 `submit_alpha.py`）

每天按这个顺序跑：

1. `python digging_1step.py`
2. `python digging_2step.py`
3. `python check.py`

你后续只需要在平台手动提交 `records/submitable_alpha.csv` 里的 alpha id。

---

## 3. 每个脚本“跑到什么程度”可以停

## 3.1 `digging_1step.py`

目标：尽快扩充一阶段候选池（冲分期优先数量和覆盖面）。

建议停点（满足任一即可）：
- 日志显示待跑数量已经明显下降（比如剩余 < 20%）。
- 连续两轮运行，新增完成数很少（比如每轮新增 < 5%）。
- 当次运行结束出现“没有因子可以跑了，请切换数据集”。

实操建议：
- 冲分期每天先跑 1~3 轮。
- 若 step1 增长变慢，优先补新 `dataset_id` 而不是死跑同一个池子。

## 3.2 `digging_2step.py`

目标：把 step1 的有效表达式扩成二阶段候选并继续回测。

建议停点（满足任一即可）：
- 日志出现“暂时没有满足条件的一阶段因子，请你继续运行digging_1step.”
- 连续两轮运行后，`scheduled` 下降明显且新增很少。
- 当天 step2 总回测量已达到你账户可稳定消化的范围（避免只堆积未检查候选）。

实操建议：
- step2 紧跟 step1 跑，不要隔太久。
- 如果 step1 当天产出很少，step2 跑 1 轮即可。

## 3.3 `check.py`

目标：持续把可提交因子筛到 `records/submitable_alpha.csv`。

特点：
- `check.py` 是 `while True` 长循环脚本，理论上可持续运行。

建议停点（满足任一即可）：
- `submitable_alpha.csv` 当天新增达到你的人工提交流量上限（比如 5~20 个）。
- 日志中“待 check 数量”持续很低，增量接近 0。
- 你准备进入手动提交与复盘阶段。

实操建议：
- 每天至少让 `check.py` 跑 1 个完整时段（例如一次连续跑到增量明显下降）。
- 你现在不用 `submit_alpha.py`，所以 `check.py` 产出的 id 是你的人工提交池。

---

## 4. 阶段切换建议（和你的目标一致）

## 阶段A：先冲 10000 分
- `digging_1step.py`：`EARLY_SCORE_MODE = True`
- `digging_2step.py`：`EARLY_SCORE_MODE = True`
- 重心：提高单位时间“可用候选增速”

## 阶段B：再提质量
- `digging_1step.py`：`EARLY_SCORE_MODE = False`
- `digging_2step.py`：`EARLY_SCORE_MODE = False`
- 重心：提高低自相关、高通过率样本占比

切换信号：
- 分数目标接近/达到后再切，不要过早切提质。

---

## 5. 每天最少复盘 4 个数字

建议记录：
1. step1 当天完成量（新增）
2. step2 当天完成量（新增）
3. check 当天新增 `submitable_alpha.csv` 数量
4. 自相关失败占比（从 check 日志估算）

用这 4 个数判断第二天策略：
- 产量低：加大 step1，补数据集
- 产量高但通过低：降速，切提质模式，观察 1~2 天

---

## 6. 常见误区

- 只跑 step1，不跑 step2/check：会积压候选，转化率低。
- step1_tag / step2_tag 不一致：step2 吃不到你的一阶段成果。
- 太早切到提质模式：会影响冲分速度。
- 不做每日最小复盘：容易盲跑。

---

## 7. 一句话执行版

每天固定：
- 先跑 `digging_1step.py`
- 再跑 `digging_2step.py`
- 最后长跑 `check.py`
- 从 `records/submitable_alpha.csv` 里做人工提交

前期先冲分，后期再提质量。