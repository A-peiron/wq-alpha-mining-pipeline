# Alpha Mining 操作手册

## 1. 连接服务器

```bash
ssh ubuntu@124.223.70.190
```

---

## 2. 进程管理（supervisorctl）

```bash
sudo supervisorctl status          # 查看所有进程状态
sudo supervisorctl restart web     # 重启 web 后端（改了 Python 代码后必须执行）
sudo supervisorctl stop web        # 停止
sudo supervisorctl start web       # 启动
```

状态说明：`RUNNING` 正常 / `STOPPED` 已停止 / `FATAL` 启动失败（看日志排查）

> **注意**：挖矿入口已统一到 Web 控制台，旧 `orchestrator.py` 已删除。digging 阶段由 Web 控制台启停。

---

## 3. 查看日志

```bash
tail -f /home/ubuntu/alpha/logs/digging.log   # 挖矿实时日志
tail -f /home/ubuntu/alpha/logs/check.log     # check 实时日志
tail -f /home/ubuntu/alpha/logs/web.log       # web 后端日志
tail -n 100 /home/ubuntu/alpha/logs/web.log   # 最后 100 行
```

> Web 控制台的"脚本控制"页也有实时日志流，日常优先看那里。

---

## 4. 挖矿脚本控制（推荐用 Web 控制台）

浏览器访问 **http://124.223.70.190:8000** → 左侧导航 → **脚本控制**

- Web 控制台从阶段注册表读取可运行阶段，当前启用 Order 1 / Order 2 / Order 3 / Check，并预留 Order 4 / Order 5
- **Order 1 / Order 2 / Order 3** 属于 digging 互斥组，同时只能运行一个（启动一个会自动停同组其他阶段）
- **Check** 可与任意 digging 阶段同时运行
- 旧接口名 `1step` / `2step` / `3step` 仍兼容，分别映射到 `order1` / `order2_group` / `order3_decay`
- 每个卡片显示"已模拟 X 个"（从去重文件统计，累计值）

### 命令行启停（备用）
```bash
# 手动启动（后台运行，日志写入 logs/digging.log）
cd /home/ubuntu/alpha
nohup python digging_2step.py >> logs/digging.log 2>&1 &

# 查找并停止
pgrep -f digging_2step.py
kill <PID>
```

---

## 5. 挖矿工作流

### 流程图
```
Order 1 广撒网        →  记录高分因子到 Brain 平台
        ↓（Sharpe > 0.75, Fitness > 0.5）
Order 2 分组增强      →  套 group 算子，生成二阶因子
        ↓（Sharpe > 1.0, Fitness > 0.7，门槛更高）
Order 3 参数增强      →  外套 ts_decay_linear / rank / zscore
        ↓
Order 4 变体扩展      →  预留：字段替换 / 窗口扰动 / 算子替换
        ↓
Order 5 相关性剪枝    →  预留：剪枝后进入人工复核队列
        ↓
check.py              →  自相关检验 → submitable_alpha.csv
        ↓
手动提交到 Brain 平台
```

### 日常执行顺序

1. 确认 check.py 在运行（可与 digging 脚本同时运行）
2. 根据阶段选择运行 step1、step2 或 step3
3. 定期查看"数据分析"页面的漏斗和产出趋势

### 每个脚本的停止时机

**step1**：
- 日志出现"没有因子可以跑了"→ 停，换数据集
- 连续两轮新增 < 5% → 考虑停

**step2**：
- 日志出现"暂时没有满足条件的一阶段因子" → 先跑 step1
- 当天产量充足时跑 1 轮即可

**step3**：
- 日志出现"暂时没有满足条件的二阶段因子" → 先跑 step2
- step2 高分因子（Sharpe > 1.0, Fitness > 0.7）积累足够后再跑

**check**：长跑，持续运行即可；submitable_alpha.csv 当天新增达到手动提交上限时可暂停

---

## 6. A/B 实验配置

Web 控制台 → **脚本控制** → 页面顶部"实验配置"栏

| 实验 | step3_tag | 说明 |
|------|-----------|------|
| baseline | `fundamental6_usa_3step` | 默认，标准 3step |
| v2_decay_outer | `fundamental6_usa_3step_v2` | 对照组，产出独立存储 |

**使用建议**：
- step1/step2 的 tag 两个实验完全一样，切换实验对它们**没有任何影响**
- 先用 **baseline** 跑一批 3step，积累数据后再切 v2_decay_outer 做横向对比
- 切换实验后下次启动脚本时生效，不影响正在运行的进程

---

## 7. 待模拟数量说明

控制台目前显示"**已模拟 X 个**"（去重文件的累计行数），**不显示待模拟数量**。

待模拟数量 = 候选总数 − 已模拟，而候选总数在脚本运行时才动态生成（需调用 Brain API 获取高分因子并展开表达式），无法提前静态查询。

**替代方案**：在控制台日志流中查看，脚本每次启动都会打印：
```
N 个因子正在等待回测，已经完成了 M 个因子
```

---

## 8. 数据分析页

浏览器 → **数据分析**，包含 5 个 Tab：

| Tab | 内容 |
|-----|------|
| 挖掘漏斗 | 模拟总数 → 成功 → 去重 → 通过 check，按 step 分组 |
| Sharpe/Fitness 分布 | 通过 check 因子的质量分布 |
| 产出时间线 | 近 7/14/30 天每日新增数，观察挖掘效率趋势 |
| 指标散点图 | Sharpe vs Fitness，点大小=Turnover，颜色区分 step |
| Top 因子表 | 按 Sharpe/Fitness/Turnover/Self-Corr 排序，支持 tag 过滤 |

**每日至少看一次**：漏斗的"通过率"和时间线的趋势，判断明天策略。

---

## 9. 阶段策略

### 阶段 A：冲分（目标 < 10000 分）
- `EARLY_SCORE_MODE = True`（digging_1step.py 和 digging_2step.py 均设置）
- 重心：提高单位时间候选增速
- 优先跑 step1 → step2，step3 视情况

### 阶段 B：提质（达到 10000 分后）
- `EARLY_SCORE_MODE = False`
- 重心：提高低自相关、高通过率样本占比
- step3 优先级提升（ts_decay_linear 可显著降低 Turnover，提升 Fitness）

---

## 10. 代码部署

### 前端更新
```bash
# 本地构建（Git Bash）
cd "c:/Users/lenovo/Desktop/世坤量化/用户级别V1.1.10/frontend/alpha-web"
npm run build

# 上传
scp -r dist/* ubuntu@124.223.70.190:/home/ubuntu/alpha/web/static/
# web 服务不需要重启（静态文件直接生效）
```

### 后端更新
```bash
# 上传后端文件
scp config.py machine_lib.py digging_1step.py digging_2step.py digging_3step.py check.py \
    experiment_config.json ubuntu@124.223.70.190:/home/ubuntu/alpha/
scp -r mining ubuntu@124.223.70.190:/home/ubuntu/alpha/
scp web/routers/control.py web/routers/status.py web/routers/experiments.py web/routers/alphas.py \
    ubuntu@124.223.70.190:/home/ubuntu/alpha/web/routers/
scp web/app.py ubuntu@124.223.70.190:/home/ubuntu/alpha/web/

# 必须重启 web 才生效
ssh ubuntu@124.223.70.190 "sudo supervisorctl restart web"
```

---

## 11. 关键文件路径

| 文件 | 服务器路径 |
|------|-----------|
| step1 挖矿 | `/home/ubuntu/alpha/digging_1step.py` |
| step2 深挖 | `/home/ubuntu/alpha/digging_2step.py` |
| step3 深挖 | `/home/ubuntu/alpha/digging_3step.py` |
| check | `/home/ubuntu/alpha/check.py` |
| 实验配置 | `/home/ubuntu/alpha/experiment_config.json` |
| 核心库 | `/home/ubuntu/alpha/machine_lib.py` |
| 阶段注册/预检/质量 | `/home/ubuntu/alpha/mining/` |
| Web 后端 | `/home/ubuntu/alpha/web/app.py` |
| 模拟记录 | `/home/ubuntu/alpha/records/simulations.jsonl` |
| 可提交因子 | `/home/ubuntu/alpha/records/submitable_alpha.csv` |
| 去重文件 | `/home/ubuntu/alpha/records/{tag}_simulated_alpha_expression.txt` |
| 挖矿日志 | `/home/ubuntu/alpha/logs/digging.log` |
| check 日志 | `/home/ubuntu/alpha/logs/check.log` |
| web 日志 | `/home/ubuntu/alpha/logs/web.log` |

---

## 12. 常见问题

### web 显示 FATAL
```bash
tail -n 50 /home/ubuntu/alpha/logs/web.log
```

### 脚本控制页面按钮无响应
检查 web 是否在运行：`sudo supervisorctl status`

### step3 启动后立即提示"暂时没有满足条件的二阶段因子"
step2 高分因子不够（需要 Sharpe > 1.0, Fitness > 0.7），先多跑 step2。

### 实验切换后 step3 还在写旧 tag
切换实验只对下次启动生效，需要先停止 step3 再重新启动。

### 想查看当前有多少待跑因子
在控制台日志流里找最近一行 `N 个因子正在等待回测`，或直接重启脚本让它重新统计。
